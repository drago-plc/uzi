query MakeTripRoute($pickup: TripInput! $dropoff: TripInput!) {
    makeTripRoute(
        input: {
            pickup: $pickup
            dropoff: $dropoff
        }
    ) {
        polyline
        availableProducts {
            id
            name
            description
            icon_url
            price
        }
    }
}