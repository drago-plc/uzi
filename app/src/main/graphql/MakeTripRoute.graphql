query ComputeTripRoute($pickup: TripInput! $dropoff: TripInput!) {
    computeTripRoute(
        input: {
            pickup: $pickup
            dropoff: $dropoff
        }
    ) {
        polyline
        availableProducts {
            id
            name
            description
            icon_url
            price
        }
    }
}