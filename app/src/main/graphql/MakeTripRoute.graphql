query MakeTripRoute($pickup: TripInput! $dropoff: TripInput!) {
    makeTripRoute(
        input: {
            pickup: $pickup
            dropoff: $dropoff
        }
    ) {
        polyline
    }
}