query GetTripDetails($id: UUID!) {
    getTripDetails(tripId: $id) {
        id
        status
        courier {
            id
            user {
                first_name
                last_name
            }
            avatar {
                uri
            }
        }
    }
}