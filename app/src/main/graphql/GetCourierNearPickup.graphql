query GetCourierNearPickupPoint($point: GpsInput!) {
    getCourierNearPickupPoint(point: $point) {
        id
        product {
            id
            name
            icon_url
        }
        location {
            lat
            lng
        }
    }
}